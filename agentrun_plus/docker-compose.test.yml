# Docker Compose override file for testing
# Usage: docker-compose -f docker-compose.yml -f docker-compose.test.yml up

services:
  python_runner:
    # Override to expose port 5000 externally for testing
    ports:
      - "5000:5000"
